name: Debug pg_dump

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Check pg_dump
        run: |
          which pg_dump
          pg_dump --version

      - name: Find all pg_dump binaries
        run: |
          find /usr -name pg_dump 2>/dev/null
